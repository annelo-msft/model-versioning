// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

using System.Collections.Generic;
using System.Text;
using System.Text.Json;
using Azure.Core;

namespace Azure.AI.TextAnalytics.Models
{
    public partial class SentenceSentiment : IUtf8JsonDeserializable
    {
        internal static SentenceSentiment DeserializeSentenceSentiment(JsonElement element)
        {
            string text = default;
            SentenceSentimentValue sentiment = default;
            SentimentConfidenceScorePerLabel confidenceScores = default;
            int offset = default;
            int length = default;
            Optional<IReadOnlyList<SentenceTarget>> targets = default;
            Optional<IReadOnlyList<SentenceAssessment>> assessments = default;
            foreach (var property in element.EnumerateObject())
            {
                if (property.NameEquals("text"))
                {
                    text = property.Value.GetString();
                    continue;
                }
                if (property.NameEquals("sentiment"))
                {
                    sentiment = property.Value.GetString().ToSentenceSentimentValue();
                    continue;
                }
                if (property.NameEquals("confidenceScores"))
                {
                    confidenceScores = SentimentConfidenceScorePerLabel.DeserializeSentimentConfidenceScorePerLabel(property.Value);
                    continue;
                }
                if (property.NameEquals("offset"))
                {
                    offset = property.Value.GetInt32();
                    continue;
                }
                if (property.NameEquals("length"))
                {
                    length = property.Value.GetInt32();
                    continue;
                }
                if (property.NameEquals("targets"))
                {
                    if (property.Value.ValueKind == JsonValueKind.Null)
                    {
                        property.ThrowNonNullablePropertyIsNull();
                        continue;
                    }
                    List<SentenceTarget> array = new List<SentenceTarget>();
                    foreach (var item in property.Value.EnumerateArray())
                    {
                        array.Add(SentenceTarget.DeserializeSentenceTarget(item));
                    }
                    targets = array;
                    continue;
                }
                if (property.NameEquals("assessments"))
                {
                    if (property.Value.ValueKind == JsonValueKind.Null)
                    {
                        property.ThrowNonNullablePropertyIsNull();
                        continue;
                    }
                    List<SentenceAssessment> array = new List<SentenceAssessment>();
                    foreach (var item in property.Value.EnumerateArray())
                    {
                        array.Add(SentenceAssessment.DeserializeSentenceAssessment(item));
                    }
                    assessments = array;
                    continue;
                }
            }
            return new SentenceSentiment(text, sentiment, confidenceScores, offset, length, Optional.ToList(targets), Optional.ToList(assessments));
        }

        internal static SentenceSentiment Deserialize(ref Utf8JsonReader reader)
        {
            IUtf8JsonDeserializable model = new SentenceSentiment();
            model.Read(ref reader);
            return (SentenceSentiment)model;
        }

        static readonly byte[] b_text = Encoding.UTF8.GetBytes("text");
        static readonly byte[] b_sentiment = Encoding.UTF8.GetBytes("sentiment");
        static readonly byte[] b_confidencescores = Encoding.UTF8.GetBytes("confidenceScores");
        static readonly byte[] b_offset = Encoding.UTF8.GetBytes("offset");
        static readonly byte[] b_length = Encoding.UTF8.GetBytes("length");
        static readonly byte[] b_targets = Encoding.UTF8.GetBytes("targets");
        static readonly byte[] b_assessments = Encoding.UTF8.GetBytes("assessments");

        void IUtf8JsonDeserializable.Read(ref Utf8JsonReader reader)
        {
            while (reader.Read())
            {
                switch (reader.TokenType)
                {
                    case JsonTokenType.StartObject:
                        break;

                    case JsonTokenType.EndObject:
                        return;

                    case JsonTokenType.StartArray:
                        break;

                    case JsonTokenType.EndArray:
                        break;

                    case JsonTokenType.PropertyName:
                        {
                            if (reader.ValueTextEquals(b_text))
                            {
                                reader.Skip();
                                Text = reader.GetString();
                                continue;
                            }

                            if (reader.ValueTextEquals(b_sentiment))
                            {
                                reader.Skip();
                                Sentiment = reader.GetString().ToSentenceSentimentValue();
                                continue;
                            }

                            if (reader.ValueTextEquals(b_confidencescores))
                            {
                                ConfidenceScores = SentimentConfidenceScorePerLabel.Deserialize(ref reader);
                                continue;
                            }

                            if (reader.ValueTextEquals(b_offset))
                            {
                                reader.Skip();
                                Offset = reader.GetInt32();
                                continue;
                            }

                            if (reader.ValueTextEquals(b_length))
                            {
                                reader.Skip();
                                Length = reader.GetInt32();
                                continue;
                            }

                            if (reader.ValueTextEquals(b_targets))
                            {
                                reader.Read();
                                var list = new List<SentenceTarget>();
                                while (reader.Read() && reader.TokenType != JsonTokenType.EndArray)
                                {
                                    list.Add(SentenceTarget.Deserialize(ref reader));
                                }
                                Targets = list.AsReadOnly();
                                continue;
                            }

                            if (reader.ValueTextEquals(b_assessments))
                            {
                                reader.Read();
                                var list = new List<SentenceAssessment>();
                                while (reader.Read() && reader.TokenType != JsonTokenType.EndArray)
                                {
                                    list.Add(SentenceAssessment.Deserialize(ref reader));
                                }
                                Assessments = list.AsReadOnly();
                                continue;
                            }
                        }
                        break;

                    default:
                        throw new NotSupportedException("Not supported token type " + reader.TokenType);
                }
            }
        }

    }
}
